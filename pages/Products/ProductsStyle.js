import { StyleSheet } from "react-native"

export default style = StyleSheet.create({
  bg: {
    backgroundColor: '#0e0c1a'
  },

  productList: {
    flex: 1,
    alignItems: 'center',
    marginTop: 65,
    gap: 25
  },
});